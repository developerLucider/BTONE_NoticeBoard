<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.btone.web.home.domain.board.mapper.BoardMapper">

	<insert id="addContent" parameterType ="com.btone.web.home.domain.board.vo.Board">
		insert into board (
		 c_no,user_no, board_title,board_content, create_date, update_date, board_hits
		) values(
		#{categoryNo},#{userNo},#{boardTitle},#{boardContent},now(),now(),0
		)
    </insert>
<<<<<<< Updated upstream
  	
  	<select id="getBoardInfo" resultType ="com.btone.web.home.domain.board.vo.Board">
  		SELECT * FROM BOARD WHERE BOARD_NO = 10
  	</select>
=======
    
    <select id="selectBoard" resultType="com.btone.web.home.domain.board.vo.BoardVO">
	SELECT 
		 BOARD_NO
		,C_NO
		,USER_NO
		,BOARD_TITLE
		,BOARD_CONTENT
		,CREATE_DATE
		,UPDATE_DATE
		,BOARD_HITS
		from board
	</select>
  
>>>>>>> Stashed changes
  
</mapper>